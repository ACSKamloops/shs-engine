# Pukaist Engine Configuration
# Copy this file to .env before running

# --- General System ---
ENV=production
LOG_LEVEL=INFO
SECRET_KEY=change_this_to_a_secure_random_string
ALLOWED_HOSTS=["*"]
CORS_ORIGINS=["http://localhost:5173", "http://localhost:80", "https://app.pukaist.io"]

# --- Database & Queue ---
# Use an absolute path for SQLite ensuring persistence
QUEUE_DB_PATH=/data/pukaist.db
# Or use Postgres (recommended for production):
# DATABASE_URL=postgresql://user:pass@db:5432/pukaist

# --- File Storage ---
# Local storage path
INCOMING_DIR=/data/uploads
WORKSPACE_DIR=/data/workspace
# Or S3/MinIO:
# STORAGE_BACKEND=s3
# S3_BUCKET=pukaist-uploads
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-west-2

# --- Search Index ---
INDEX_PATH=/data/index_v1
HNSW_M=16
HNSW_EF_CONSTRUCTION=200

# --- LLM & AI ---
# Provider: openai, anthropic, or ollama (local)
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-your-openai-key
# ANTHROPIC_API_KEY=
# OLLAMA_BASE_URL=http://host.docker.internal:11434

# Features
LLM_SUMMARY_ENABLED=true
LLM_INSIGHTS_ENABLED=true
LLM_FORENSIC_ENABLED=false

# --- OCR & Vision ---
# Backend: tesseract (local) or hunyuan (requires GPU/API)
OCR_BACKEND=tesseract
# HUNYUAN_API_URL=

# --- Geo Integration ---
PLACE_SUGGEST_ENABLED=true
# Optional: External Geocoding API key if needed
# GEOCODING_API_KEY=

# --- Auth & Roles ---
# Enable role-based access control
ROLES_CLAIM=roles
ROLE_ADMIN=admin
ROLE_INGEST=editor
# JWT Configuration
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=1440
